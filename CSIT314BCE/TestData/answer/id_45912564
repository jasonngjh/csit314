<p><strong>Single Page Application</strong>, within the same application route</p>

<pre><code>window.location.pathname = '/stack';
</code></pre>

<p><strong>JavaScript:</strong></p>

<pre><code>location.href = "http://stack.com";
window.location = "http://stack.com";
</code></pre>

<p><strong>jQuery:</strong></p>

<pre><code>$(location).attr('href', "http://www.stack.com");
$(window).attr('location', "http://www.stack.com");
</code></pre>

<p><strong>Angular 4</strong></p>

<pre><code>import { Router } from '@angular/router';
export class NavtabComponent{
    constructor(private router: Router) {
    }
    this.router.navigate(['bookings/taxi']);
}
</code></pre>
