<p>It's just the limiting case of object destructuring.</p>

<p>While it may seem pointless to allow it, allowing edge cases like this is useful for automatically generated code, so the generator doesn't have to check that there are zero items and skip generating the code.</p>
