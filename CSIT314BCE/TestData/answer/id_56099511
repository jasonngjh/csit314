<p><em>A neat way to chain multiple operators:</em></p>

<pre><code>f = lambda x,y: 'greater' if x &gt; y else 'less' if y &gt; x else 'equal'

array = [(0,0),(0,1),(1,0),(1,1)]

for a in array:
  x, y = a[0], a[1]
  print(f(x,y))

# Output is:
#   equal,
#   less,
#   greater,
#   equal

</code></pre>
