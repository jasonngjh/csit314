<p>Utterly geek, but I will be using this:</p>

<pre><code>#define as ;while

int main(int argc, char* argv[])
{
    int n = atoi(argv[1]);
    do printf("n is %d\n", n) as ( n --&gt; 0);
    return 0;
}
</code></pre>
