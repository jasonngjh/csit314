<p>Using ES6:</p>

<p>(Destructuring + Spread operator)</p>

<pre class="lang-js prettyprint-override"><code>const myObject = {
    regex: "^http://.*",
    b: 2,
    c: 3
};
const { regex, ...noRegex } = myObject;
console.log(noRegex); // =&gt; { b: 2, c: 3 }
</code></pre>
